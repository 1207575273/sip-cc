# 多显示器支持实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 sip-cc 在 Windows/Linux 上支持多显示器截屏、GIF 录制、视频录制（含跨屏选区），在 macOS 上支持多显示器独立截屏/录制。

**Architecture:** 平台分治——Windows/Linux 用单个 overlay 覆盖虚拟桌面（支持跨屏），macOS 用每个显示器独立 overlay。Rust 端新增虚拟桌面拼接截取能力，前端 overlay 适配虚拟桌面坐标系。

**Tech Stack:** Rust (Tauri v2, xcap 0.9, image 0.25) + TypeScript (Tauri API)

**Design Doc:** `docs/20260410240000_多显示器支持设计文档.md`

---

## File Structure

| 文件 | 职责 | 改动类型 |
|------|------|----------|
| `src-tauri/src/snap/capture.rs` | 屏幕捕获核心：多显示器枚举、虚拟桌面拼接、跨屏截取 | 重构 |
| `src-tauri/src/commands/snap_cmd.rs` | overlay 管理、坐标转换、截屏命令、`get_screen_info` 命令 | 修改 |
| `src-tauri/src/lib.rs` | overlay 窗口预创建（平台分治） | 修改 |
| `src-tauri/src/video/session.rs` | 视频录制会话（坐标改 i32，用 `capture_virtual_region`） | 修改 |
| `src-tauri/src/gif/frame.rs` | GIF 录制会话（同上） | 修改 |
| `src-tauri/src/commands/video_cmd.rs` | 视频录制启动坐标转换 | 修改 |
| `src-tauri/src/commands/gif_cmd.rs` | GIF 录制启动坐标转换 | 修改 |
| `src-tauri/src/tray.rs` | 退出时清理所有 overlay-N 窗口 | 修改 |
| `ui/shared/invoke.ts` | 新增 `callGetScreenInfo` | 修改 |
| `ui/views/SelectionOverlay.ts` | Canvas 尺寸不变（已用 window.innerWidth），无需改动 | 无改动 |
| `ui/views/SnapOverlay.ts` | 无改动（坐标由 overlay 窗口 position 决定） | 无改动 |
| `ui/views/GifOverlay.ts` | 无改动 | 无改动 |
| `ui/views/VideoOverlay.ts` | 无改动 | 无改动 |

> **关键洞察**：前端 `SelectionOverlay.ts` 用 `window.innerWidth / innerHeight` 做 Canvas 尺寸，用 `e.clientX / clientY` 做选区坐标——这些都是相对于 overlay 窗口的。只要 Rust 端把 overlay 窗口正确定位到虚拟桌面 bounding box，前端选区坐标天然就是虚拟桌面逻辑坐标，**前端 overlay 代码不需要任何改动**。坐标偏移由 Rust 端的 overlay 窗口 position 隐式处理。

---

### Task 1: capture.rs — 几何工具 + 多显示器枚举

**Files:**
- Modify: `src-tauri/src/snap/capture.rs`

- [ ] **Step 1: 新增几何结构体和多显示器枚举函数**

在 `capture.rs` 顶部新增以下内容（保留已有代码不变）：

```rust
use image::{RgbaImage, Rgba};
use xcap::Monitor;

/// 虚拟桌面 bounding box（物理像素坐标）
#[derive(Debug, Clone)]
pub struct VirtualScreenBounds {
    pub x: i32,
    pub y: i32,
    pub width: u32,
    pub height: u32,
}

/// 矩形区域（用于相交计算）
#[derive(Debug, Clone, Copy)]
pub struct PhysRect {
    pub x: i32,
    pub y: i32,
    pub w: i32,
    pub h: i32,
}

impl PhysRect {
    /// 计算两个矩形的交集，无交集返回 None
    pub fn intersect(&self, other: &PhysRect) -> Option<PhysRect> {
        let x1 = self.x.max(other.x);
        let y1 = self.y.max(other.y);
        let x2 = (self.x + self.w).min(other.x + other.w);
        let y2 = (self.y + self.h).min(other.y + other.h);
        if x2 > x1 && y2 > y1 {
            Some(PhysRect { x: x1, y: y1, w: x2 - x1, h: y2 - y1 })
        } else {
            None
        }
    }
}

pub struct ScreenCapture {
    pub image: RgbaImage,
}

/// 获取所有显示器
pub fn get_all_monitors() -> Result<Vec<Monitor>, String> {
    Monitor::all().map_err(|e| format!("获取显示器列表失败: {e}"))
}

/// 获取主显示器
fn get_primary_monitor() -> Result<Monitor, String> {
    get_all_monitors()?
        .into_iter()
        .find(|m| m.is_primary().unwrap_or(false))
        .ok_or_else(|| "未找到主显示器".to_string())
}

/// 计算所有显示器的虚拟桌面 bounding box
pub fn get_virtual_screen_bounds() -> Result<VirtualScreenBounds, String> {
    let monitors = get_all_monitors()?;
    if monitors.is_empty() {
        return Err("没有检测到任何显示器".to_string());
    }
    let min_x = monitors.iter().map(|m| m.x()).min().unwrap();
    let min_y = monitors.iter().map(|m| m.y()).min().unwrap();
    let max_x = monitors.iter().map(|m| m.x() + m.width() as i32).max().unwrap();
    let max_y = monitors.iter().map(|m| m.y() + m.height() as i32).max().unwrap();
    Ok(VirtualScreenBounds {
        x: min_x,
        y: min_y,
        width: (max_x - min_x) as u32,
        height: (max_y - min_y) as u32,
    })
}

/// 根据虚拟桌面物理坐标查找所在显示器
pub fn find_monitor_at(x: i32, y: i32) -> Result<Monitor, String> {
    let monitors = get_all_monitors()?;
    for m in &monitors {
        if x >= m.x() && x < m.x() + m.width() as i32
            && y >= m.y() && y < m.y() + m.height() as i32
        {
            return Ok(m.clone());
        }
    }
    // 回退到主显示器
    get_primary_monitor()
}

/// 判断选区是否完全在单个显示器内（快速路径）
fn find_single_containing_monitor(
    monitors: &[Monitor], x: i32, y: i32, w: u32, h: u32,
) -> Option<usize> {
    monitors.iter().position(|m| {
        x >= m.x()
            && y >= m.y()
            && x + w as i32 <= m.x() + m.width() as i32
            && y + h as i32 <= m.y() + m.height() as i32
    })
}
```

- [ ] **Step 2: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过（可能有 unused 警告，无 error）

- [ ] **Step 3: 新增 PhysRect::intersect 单元测试**

在 `capture.rs` 底部新增：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn should_intersect_overlapping_rects() {
        let a = PhysRect { x: 0, y: 0, w: 100, h: 100 };
        let b = PhysRect { x: 50, y: 50, w: 100, h: 100 };
        let result = a.intersect(&b).unwrap();
        assert_eq!(result.x, 50);
        assert_eq!(result.y, 50);
        assert_eq!(result.w, 50);
        assert_eq!(result.h, 50);
    }

    #[test]
    fn should_return_none_for_non_overlapping() {
        let a = PhysRect { x: 0, y: 0, w: 100, h: 100 };
        let b = PhysRect { x: 200, y: 200, w: 100, h: 100 };
        assert!(a.intersect(&b).is_none());
    }

    #[test]
    fn should_handle_contained_rect() {
        let outer = PhysRect { x: 0, y: 0, w: 200, h: 200 };
        let inner = PhysRect { x: 50, y: 50, w: 50, h: 50 };
        let result = outer.intersect(&inner).unwrap();
        assert_eq!(result.x, 50);
        assert_eq!(result.y, 50);
        assert_eq!(result.w, 50);
        assert_eq!(result.h, 50);
    }

    #[test]
    fn should_handle_edge_touching() {
        let a = PhysRect { x: 0, y: 0, w: 100, h: 100 };
        let b = PhysRect { x: 100, y: 0, w: 100, h: 100 };
        // 边缘相切不算相交
        assert!(a.intersect(&b).is_none());
    }

    #[test]
    fn should_handle_negative_coordinates() {
        let a = PhysRect { x: -1920, y: 0, w: 1920, h: 1080 };
        let b = PhysRect { x: -100, y: 0, w: 200, h: 500 };
        let result = a.intersect(&b).unwrap();
        assert_eq!(result.x, -100);
        assert_eq!(result.y, 0);
        assert_eq!(result.w, 100); // -100..0 内的部分
        assert_eq!(result.h, 500);
    }
}
```

- [ ] **Step 4: 运行测试**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo test --lib snap::capture::tests -- --nocapture`
Expected: 5 tests passed

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/snap/capture.rs
git commit -m "feat: add geometry helpers and multi-monitor enumeration in capture.rs"
```

---

### Task 2: capture.rs — 虚拟桌面截取（capture_virtual_screen + capture_virtual_region）

**Files:**
- Modify: `src-tauri/src/snap/capture.rs`

- [ ] **Step 1: 新增 capture_screen 和 capture_region_from**

在 `capture.rs` 中原有的 `capture_primary_screen` 和 `capture_region` 之后新增：

```rust
/// 截取指定显示器全屏
pub fn capture_screen(monitor: &Monitor) -> Result<ScreenCapture, String> {
    let image = monitor
        .capture_image()
        .map_err(|e| format!("屏幕截取失败: {e}"))?;
    Ok(ScreenCapture { image })
}

/// 从指定显示器截取局部区域
pub fn capture_region_from(
    monitor: &Monitor, x: u32, y: u32, width: u32, height: u32,
) -> Result<RgbaImage, String> {
    monitor
        .capture_region(x, y, width, height)
        .map_err(|e| format!("区域截取失败: {e}"))
}
```

- [ ] **Step 2: 新增 capture_virtual_screen（拼接所有显示器为大图）**

```rust
/// 截取所有显示器并拼接为虚拟桌面大图
pub fn capture_virtual_screen() -> Result<(RgbaImage, VirtualScreenBounds), String> {
    let monitors = get_all_monitors()?;
    let bounds = get_virtual_screen_bounds()?;

    let mut canvas = RgbaImage::from_pixel(
        bounds.width, bounds.height, Rgba([0, 0, 0, 255]),
    );

    for monitor in &monitors {
        let image = monitor
            .capture_image()
            .map_err(|e| format!("截取显示器失败: {e}"))?;
        let offset_x = (monitor.x() - bounds.x) as i64;
        let offset_y = (monitor.y() - bounds.y) as i64;
        image::imageops::overlay(&mut canvas, &image, offset_x, offset_y);
    }

    Ok((canvas, bounds))
}
```

- [ ] **Step 3: 新增 capture_virtual_region（跨屏区域截取，含快速路径优化）**

```rust
/// 从虚拟桌面坐标截取区域（可跨屏）
/// x, y 是虚拟桌面物理坐标
pub fn capture_virtual_region(
    x: i32, y: i32, width: u32, height: u32,
) -> Result<RgbaImage, String> {
    let monitors = get_all_monitors()?;

    // 快速路径：选区完全在单个显示器内
    if let Some(idx) = find_single_containing_monitor(&monitors, x, y, width, height) {
        let m = &monitors[idx];
        let local_x = (x - m.x()) as u32;
        let local_y = (y - m.y()) as u32;
        return capture_region_from(m, local_x, local_y, width, height);
    }

    // 跨屏路径：从每个涉及的显示器截取重叠部分并拼接
    let mut canvas = RgbaImage::from_pixel(width, height, Rgba([0, 0, 0, 255]));
    let sel = PhysRect { x, y, w: width as i32, h: height as i32 };

    for monitor in &monitors {
        let mon = PhysRect {
            x: monitor.x(),
            y: monitor.y(),
            w: monitor.width() as i32,
            h: monitor.height() as i32,
        };
        if let Some(overlap) = sel.intersect(&mon) {
            let local_x = (overlap.x - monitor.x()) as u32;
            let local_y = (overlap.y - monitor.y()) as u32;
            let region = capture_region_from(
                monitor, local_x, local_y, overlap.w as u32, overlap.h as u32,
            )?;
            let canvas_x = (overlap.x - x) as i64;
            let canvas_y = (overlap.y - y) as i64;
            image::imageops::overlay(&mut canvas, &region, canvas_x, canvas_y);
        }
    }

    Ok(canvas)
}
```

- [ ] **Step 4: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/snap/capture.rs
git commit -m "feat: add virtual screen capture and cross-monitor region capture"
```

---

### Task 3: snap_cmd.rs — ScreenBuffer 扩展 + get_screen_info 命令

**Files:**
- Modify: `src-tauri/src/commands/snap_cmd.rs`
- Modify: `src-tauri/src/lib.rs` （注册新命令）

- [ ] **Step 1: 更新 ScreenBuffer 结构体，新增 bounds 字段**

在 `snap_cmd.rs` 中修改 `ScreenBuffer`：

```rust
use crate::snap::capture::VirtualScreenBounds;

pub struct ScreenBuffer {
    pub image: Mutex<Option<RgbaImage>>,
    pub bounds: Mutex<Option<VirtualScreenBounds>>,
}
```

- [ ] **Step 2: 新增 MonitorInfo 和 get_screen_info 命令**

在 `snap_cmd.rs` 中新增：

```rust
use serde::Serialize;

#[derive(Debug, Serialize)]
pub struct MonitorInfo {
    pub x: i32,
    pub y: i32,
    pub width: u32,
    pub height: u32,
    pub scale_factor: f64,
    pub is_primary: bool,
}

#[tauri::command]
pub fn get_screen_info(app: AppHandle) -> Result<Vec<MonitorInfo>, String> {
    let monitors = app.available_monitors().map_err(|e| e.to_string())?;
    let primary = app.primary_monitor().ok().flatten();
    let primary_pos = primary.as_ref().map(|p| p.position().clone());

    let result: Vec<MonitorInfo> = monitors
        .iter()
        .map(|m| {
            let is_primary = primary_pos
                .as_ref()
                .map(|pp| pp.x == m.position().x && pp.y == m.position().y)
                .unwrap_or(false);
            let phys = m.size();
            MonitorInfo {
                x: m.position().x,
                y: m.position().y,
                width: phys.width,
                height: phys.height,
                scale_factor: m.scale_factor(),
                is_primary,
            }
        })
        .collect();

    Ok(result)
}
```

- [ ] **Step 3: 更新 lib.rs 中的 ScreenBuffer 初始化和命令注册**

在 `lib.rs` 中修改 `ScreenBuffer` 初始化：

```rust
.manage(ScreenBuffer {
    image: Mutex::new(None),
    bounds: Mutex::new(None),
})
```

在 `invoke_handler` 中新增 `get_screen_info`：

```rust
commands::snap_cmd::get_screen_info,
```

- [ ] **Step 4: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/commands/snap_cmd.rs src-tauri/src/lib.rs
git commit -m "feat: extend ScreenBuffer with bounds, add get_screen_info command"
```

---

### Task 4: snap_cmd.rs — overlay 显示/隐藏平台分治

**Files:**
- Modify: `src-tauri/src/commands/snap_cmd.rs`

- [ ] **Step 1: 重写 show_overlay 函数，平台分治**

将现有的 `show_overlay` 替换为：

```rust
use crate::snap::capture;

fn show_overlay(app: &AppHandle) -> Result<(), String> {
    if cfg!(target_os = "macos") {
        show_overlay_per_monitor(app)
    } else {
        show_overlay_virtual_desktop(app)
    }
}

/// Windows/Linux：单 overlay 覆盖虚拟桌面
fn show_overlay_virtual_desktop(app: &AppHandle) -> Result<(), String> {
    let bounds = capture::get_virtual_screen_bounds()?;
    let win = app.get_webview_window("overlay")
        .ok_or("overlay 窗口不存在")?;

    let scale = get_scale_factor(app);
    let logical_x = bounds.x as f64 / scale;
    let logical_y = bounds.y as f64 / scale;
    let logical_w = bounds.width as f64 / scale;
    let logical_h = bounds.height as f64 / scale;

    let _ = win.set_position(tauri::Position::Logical(
        tauri::LogicalPosition::new(logical_x, logical_y),
    ));
    let _ = win.set_size(tauri::Size::Logical(
        tauri::LogicalSize::new(logical_w, logical_h),
    ));
    let _ = win.set_always_on_top(true);
    win.show().map_err(|e| format!("显示 overlay 失败: {e}"))?;
    let _ = win.set_focus();
    Ok(())
}

/// macOS：显示所有 overlay-N 窗口
fn show_overlay_per_monitor(app: &AppHandle) -> Result<(), String> {
    let monitors: Vec<_> = app.available_monitors()
        .map_err(|e| e.to_string())?
        .into_iter()
        .collect();

    for (i, monitor) in monitors.iter().enumerate() {
        let label = format!("overlay-{}", i);
        if let Some(win) = app.get_webview_window(&label) {
            let phys = monitor.size();
            let scale = monitor.scale_factor();
            let pos = monitor.position();
            let logical_x = pos.x as f64 / scale;
            let logical_y = pos.y as f64 / scale;
            let logical_w = phys.width as f64 / scale;
            let logical_h = phys.height as f64 / scale;

            let _ = win.set_position(tauri::Position::Logical(
                tauri::LogicalPosition::new(logical_x, logical_y),
            ));
            let _ = win.set_size(tauri::Size::Logical(
                tauri::LogicalSize::new(logical_w, logical_h),
            ));
            let _ = win.set_always_on_top(true);
            let _ = win.show();
        }
    }

    // 聚焦第一个 overlay（后续可优化为聚焦鼠标所在的）
    if let Some(win) = app.get_webview_window("overlay-0") {
        let _ = win.set_focus();
    }

    Ok(())
}
```

- [ ] **Step 2: 重写 hide_overlay 函数**

将现有的 `hide_overlay` 替换为：

```rust
const MAX_OVERLAY_MONITORS: usize = 8;

pub fn hide_overlay(app: &AppHandle) {
    if cfg!(target_os = "macos") {
        // macOS：隐藏所有 overlay-N
        for i in 0..MAX_OVERLAY_MONITORS {
            let label = format!("overlay-{}", i);
            if let Some(win) = app.get_webview_window(&label) {
                let _ = win.set_always_on_top(false);
                let _ = win.hide();
            }
        }
    } else {
        // Windows/Linux：隐藏单个 overlay
        if let Some(win) = app.get_webview_window("overlay") {
            let _ = win.set_always_on_top(false);
            let _ = win.hide();
        }
    }
}
```

- [ ] **Step 3: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/commands/snap_cmd.rs
git commit -m "feat: platform-split overlay show/hide for multi-monitor support"
```

---

### Task 5: lib.rs — overlay 预创建平台分治

**Files:**
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 重写 setup 中的 overlay 创建逻辑**

将 `lib.rs` 中 `.setup(|app| { ... })` 的 overlay 创建部分替换为：

```rust
.setup(|app| {
    let wal = app.state::<WalLogger>();
    wal.info("APP", "应用启动");
    tray::create_tray(app.handle())?;
    hotkey::register::start_hotkey_listener(app.handle().clone());

    if cfg!(target_os = "macos") {
        // macOS 方案 B：为每个显示器创建一个 overlay
        let monitors: Vec<_> = app.available_monitors()
            .map_err(|e| Box::new(std::io::Error::new(std::io::ErrorKind::Other, e.to_string())))?
            .into_iter()
            .collect();
        for (i, _monitor) in monitors.iter().enumerate() {
            let label = format!("overlay-{}", i);
            let url = format!("index.html?view=snap-overlay&monitor={}", i);
            WebviewWindowBuilder::new(
                app,
                &label,
                WebviewUrl::App(url.into()),
            )
            .title("sip-cc overlay")
            .transparent(true)
            .decorations(false)
            .visible(false)
            .build()?;
        }
        wal.info("APP", &format!("macOS: {} 个 overlay 窗口预创建完成", monitors.len()));
    } else {
        // Windows/Linux 方案 A：单个 overlay，不用 fullscreen
        WebviewWindowBuilder::new(
            app,
            "overlay",
            WebviewUrl::App("index.html?view=snap-overlay".into()),
        )
        .title("sip-cc overlay")
        .transparent(true)
        .decorations(false)
        .visible(false)
        .build()?;
        wal.info("APP", "overlay 窗口预创建完成");
    }

    Ok(())
})
```

- [ ] **Step 2: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/lib.rs
git commit -m "feat: platform-split overlay window creation in setup"
```

---

### Task 6: snap_cmd.rs — open_snap_overlay + snap_region 虚拟桌面支持

**Files:**
- Modify: `src-tauri/src/commands/snap_cmd.rs`

- [ ] **Step 1: 重写 open_snap_overlay**

将现有的 `open_snap_overlay` 替换为：

```rust
pub fn open_snap_overlay(app: &AppHandle) -> Result<(), String> {
    let wal = app.state::<WalLogger>();
    wal.info("SNAP", "截屏触发，预截屏幕");

    let buffer = app.state::<ScreenBuffer>();

    if cfg!(target_os = "macos") {
        // macOS：截取主显示器全屏（后续可优化为鼠标所在显示器）
        let screen = crate::snap::capture::capture_primary_screen()?;
        let primary = app.primary_monitor().ok().flatten()
            .ok_or("未找到主显示器")?;
        let pos = primary.position();
        let phys = primary.size();
        *buffer.image.lock().map_err(|e| e.to_string())? = Some(screen.image);
        *buffer.bounds.lock().map_err(|e| e.to_string())? = Some(
            crate::snap::capture::VirtualScreenBounds {
                x: pos.x, y: pos.y,
                width: phys.width, height: phys.height,
            }
        );
    } else {
        // Windows/Linux：截取所有显示器拼接为虚拟桌面大图
        let (image, bounds) = crate::snap::capture::capture_virtual_screen()?;
        wal.info("SNAP", &format!(
            "虚拟桌面预截完成 | bounds={},{},{},{}", 
            bounds.x, bounds.y, bounds.width, bounds.height
        ));
        *buffer.image.lock().map_err(|e| e.to_string())? = Some(image);
        *buffer.bounds.lock().map_err(|e| e.to_string())? = Some(bounds);
    }

    show_overlay(app)?;
    std::thread::sleep(std::time::Duration::from_millis(OVERLAY_SHOW_DELAY_MS));
    let _ = app.emit("overlay-mode", "snap-overlay");
    Ok(())
}
```

- [ ] **Step 2: 确认 snap_region 无需改动**

当前 `snap_region` 的逻辑是：`region 逻辑坐标 × scale → 物理坐标 → 从 ScreenBuffer.image 裁剪`。

由于 Windows/Linux 下 `ScreenBuffer.image` 现在是虚拟桌面大图，`overlay 窗口的 position` 是虚拟桌面 bounding box 的逻辑原点，`e.clientX/clientY` 产生的前端选区坐标已经是相对于虚拟桌面原点的逻辑偏移——乘以 scale 后直接就是大图内的物理偏移，`crop_rgba` 从大图中裁剪即可。

**snap_region 不需要改动**，跨屏截取由预截拼接大图天然支持。

- [ ] **Step 3: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/commands/snap_cmd.rs
git commit -m "feat: open_snap_overlay captures virtual screen for cross-monitor snap"
```

---

### Task 7: video/session.rs — 坐标改 i32 + 使用 capture_virtual_region

**Files:**
- Modify: `src-tauri/src/video/session.rs`

- [ ] **Step 1: 修改 start 函数签名和内部逻辑**

将 `session.rs` 的 `start` 函数参数类型从 `u32` 改为 `i32`，截取调用改为 `capture_virtual_region`：

```rust
pub fn start(
    x: i32,
    y: i32,
    width: u32,
    height: u32,
    fps: u16,
    max_duration_secs: u64,
    tmp_dir: PathBuf,
) -> Result<Self, String> {
    std::fs::create_dir_all(&tmp_dir)
        .map_err(|e| format!("创建临时目录失败: {e}"))?;

    let state = Arc::new(AtomicU8::new(STATE_RECORDING));
    let state_clone = state.clone();

    let handle = thread::spawn(move || {
        let frame_interval = Duration::from_millis(1000 / fps as u64);
        let max_duration = Duration::from_secs(max_duration_secs);
        let start_time = Instant::now();
        let mut frame_count: u32 = 0;

        loop {
            let current_state = state_clone.load(Ordering::Relaxed);
            if current_state == STATE_STOPPED {
                break;
            }
            if start_time.elapsed() >= max_duration {
                break;
            }

            if current_state == STATE_PAUSED {
                thread::sleep(Duration::from_millis(PAUSE_SLEEP_MS));
                continue;
            }

            let frame_start = Instant::now();
            frame_count += 1;

            match capture::capture_virtual_region(x, y, width, height) {
                Ok(image) => {
                    let frame_path = tmp_dir.join(format!("frame_{:06}.png", frame_count));
                    if let Err(e) = image.save(&frame_path) {
                        eprintln!("视频帧保存失败: {e}");
                        break;
                    }
                }
                Err(e) => {
                    eprintln!("视频截屏失败: {e}");
                    frame_count -= 1;
                    continue;
                }
            }

            let elapsed = frame_start.elapsed();
            if elapsed < frame_interval {
                thread::sleep(frame_interval - elapsed);
            }
        }

        Ok((tmp_dir, frame_count))
    });

    Ok(Self {
        state,
        handle: Some(handle),
    })
}
```

- [ ] **Step 2: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过（video_cmd.rs 调用处可能出类型不匹配，下一个 Task 处理）

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/video/session.rs
git commit -m "feat: video session uses virtual region capture with i32 coordinates"
```

---

### Task 8: gif/frame.rs — 坐标改 i32 + 使用 capture_virtual_region

**Files:**
- Modify: `src-tauri/src/gif/frame.rs`

- [ ] **Step 1: 修改 start 函数签名和内部逻辑**

将 `frame.rs` 的 `start` 函数参数类型从 `u32` 改为 `i32`，截取调用改为 `capture_virtual_region`：

```rust
pub fn start(
    x: i32,
    y: i32,
    width: u32,
    height: u32,
    fps: u16,
    max_duration_secs: u64,
    output_path: PathBuf,
) -> Result<Self, String> {
    let state = Arc::new(AtomicU8::new(STATE_RECORDING));
    let state_clone = state.clone();
    let path = output_path.clone();

    let handle = thread::spawn(move || {
        let mut encoder = GifEncoder::new(&path, width as u16, height as u16, fps)?;
        let frame_interval = Duration::from_millis(1000 / fps as u64);
        let max_duration = Duration::from_secs(max_duration_secs);
        let start_time = Instant::now();

        loop {
            let current_state = state_clone.load(Ordering::Relaxed);
            if current_state == STATE_STOPPED { break; }
            if start_time.elapsed() >= max_duration { break; }

            if current_state == STATE_PAUSED {
                thread::sleep(Duration::from_millis(PAUSE_SLEEP_MS));
                continue;
            }

            let frame_start = Instant::now();

            match capture::capture_virtual_region(x, y, width, height) {
                Ok(image) => {
                    if let Err(e) = encoder.add_frame(
                        image.as_raw(),
                        image.width() as u16,
                        image.height() as u16,
                    ) {
                        eprintln!("GIF 写入帧失败: {e}");
                        break;
                    }
                }
                Err(e) => {
                    eprintln!("GIF 截屏失败: {e}");
                    continue;
                }
            }

            let elapsed = frame_start.elapsed();
            if elapsed < frame_interval {
                thread::sleep(frame_interval - elapsed);
            }
        }

        Ok(path)
    });

    Ok(Self {
        state,
        handle: Some(handle),
    })
}
```

- [ ] **Step 2: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过（gif_cmd.rs 调用处类型不匹配，下一个 Task 处理）

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/gif/frame.rs
git commit -m "feat: gif session uses virtual region capture with i32 coordinates"
```

---

### Task 9: video_cmd.rs + gif_cmd.rs — 坐标转换适配虚拟桌面

**Files:**
- Modify: `src-tauri/src/commands/video_cmd.rs`
- Modify: `src-tauri/src/commands/gif_cmd.rs`

- [ ] **Step 1: 修改 video_cmd.rs 的 video_start 坐标计算**

在 `video_start` 中，将物理坐标计算改为使用虚拟桌面偏移。关键改动——前端传来的 region 坐标是 overlay 窗口内的逻辑坐标，overlay 窗口已经定位到虚拟桌面原点，所以 `region.x * scale` 就是虚拟桌面中的物理偏移。但需要加上虚拟桌面原点才是绝对物理坐标：

```rust
// 替换 video_start 中的物理坐标计算部分
let scale = get_scale_factor(&app);
let pad = 10.0_f64;

// 获取虚拟桌面偏移（overlay 窗口 position 对应的物理原点）
let vbounds = crate::snap::capture::get_virtual_screen_bounds()
    .unwrap_or(crate::snap::capture::VirtualScreenBounds { x: 0, y: 0, width: 0, height: 0 });

// 前端逻辑坐标 → 虚拟桌面物理坐标
let phys_x = ((region.x as f64 - pad).max(0.0) * scale) as i32 + vbounds.x;
let phys_y = ((region.y as f64 - pad).max(0.0) * scale) as i32 + vbounds.y;
let phys_w = ((region.width as f64 + pad * 2.0) * scale) as u32 & !1;
let phys_h = ((region.height as f64 + pad * 2.0) * scale) as u32 & !1;
```

同时修改 `VideoRecordingSession::start` 的调用参数类型对齐：

```rust
let session =
    VideoRecordingSession::start(phys_x, phys_y, phys_w, phys_h, fps, max_duration, tmp_dir)?;
```

- [ ] **Step 2: 修改 gif_cmd.rs 的 gif_start 坐标计算**

同样的改动：

```rust
// 替换 gif_start 中的物理坐标计算部分
let scale = get_scale_factor(&app);
let pad = 10.0_f64;

let vbounds = crate::snap::capture::get_virtual_screen_bounds()
    .unwrap_or(crate::snap::capture::VirtualScreenBounds { x: 0, y: 0, width: 0, height: 0 });

let phys_x = ((region.x as f64 - pad).max(0.0) * scale) as i32 + vbounds.x;
let phys_y = ((region.y as f64 - pad).max(0.0) * scale) as i32 + vbounds.y;
let phys_w = ((region.width as f64 + pad * 2.0) * scale) as u32;
let phys_h = ((region.height as f64 + pad * 2.0) * scale) as u32;
```

同时修改 `RecordingSession::start` 的调用参数类型对齐：

```rust
let session = RecordingSession::start(
    phys_x, phys_y, phys_w, phys_h,
    fps, max_duration, output_path,
)?;
```

- [ ] **Step 3: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过，零 error

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/commands/video_cmd.rs src-tauri/src/commands/gif_cmd.rs
git commit -m "feat: video and gif commands use virtual desktop coordinates"
```

---

### Task 10: tray.rs — 退出时清理所有 overlay 窗口

**Files:**
- Modify: `src-tauri/src/tray.rs`

- [ ] **Step 1: 更新 quit 分支中的窗口清理逻辑**

在 `handle_menu_event` 的 `"quit"` 分支中，将硬编码的 `"overlay"` 改为同时清理所有 `overlay-N`：

```rust
"quit" => {
    wal.info("APP", "应用退出");

    // 清理所有可能的 overlay 窗口
    // Windows/Linux: "overlay"
    if let Some(win) = app.get_webview_window("overlay") {
        let _ = win.destroy();
    }
    // macOS: "overlay-0", "overlay-1", ...
    for i in 0..8 {
        let label = format!("overlay-{}", i);
        if let Some(win) = app.get_webview_window(&label) {
            let _ = win.destroy();
        }
    }

    for label in &["record-bar", "record-region", "record-info", "ffmpeg-download"] {
        if let Some(win) = app.get_webview_window(label) {
            let _ = win.destroy();
        }
    }
    crate::request_exit();
    app.exit(0);
}
```

- [ ] **Step 2: 编译验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo check 2>&1 | head -20`
Expected: 编译通过

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add src-tauri/src/tray.rs
git commit -m "fix: quit cleans up all overlay windows including per-monitor overlays"
```

---

### Task 11: 前端 — invoke.ts 新增 getScreenInfo

**Files:**
- Modify: `ui/shared/invoke.ts`

- [ ] **Step 1: 新增 MonitorInfo 类型和 callGetScreenInfo 函数**

在 `invoke.ts` 末尾新增：

```typescript
export interface MonitorInfo {
  x: number;
  y: number;
  width: number;
  height: number;
  scale_factor: number;
  is_primary: boolean;
}

export async function callGetScreenInfo(): Promise<MonitorInfo[]> {
  return invoke<MonitorInfo[]>("get_screen_info");
}
```

- [ ] **Step 2: Commit**

```bash
cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc"
git add ui/shared/invoke.ts
git commit -m "feat: add getScreenInfo frontend bridge"
```

---

### Task 12: 全量编译 + 构建验证

**Files:** 无新改动，验证性步骤

- [ ] **Step 1: 全量 Rust 编译**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo build 2>&1 | tail -5`
Expected: `Finished` 无 error

- [ ] **Step 2: 运行所有 Rust 测试**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc/src-tauri" && cargo test 2>&1`
Expected: 所有测试通过

- [ ] **Step 3: 前端类型检查**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc" && npx tsc --noEmit 2>&1 | tail -10`
Expected: 无类型错误

- [ ] **Step 4: Tauri dev 启动验证**

Run: `cd "C:/Users/ThinkPad/Desktop/新建文件夹 (3)/sip-cc" && npx tauri build 2>&1 | tail -10`
Expected: 构建成功

---

## 实现顺序依赖图

```
Task 1 (capture 几何+枚举)
  ↓
Task 2 (capture 虚拟桌面截取)
  ↓
Task 3 (ScreenBuffer + get_screen_info)  ←── Task 11 (前端 invoke)
  ↓
Task 4 (overlay show/hide 平台分治)
  ↓
Task 5 (lib.rs overlay 创建)  →  Task 10 (tray 清理)
  ↓
Task 6 (open_snap_overlay 虚拟桌面)
  ↓
Task 7 (video session i32)
  ↓
Task 8 (gif frame i32)
  ↓
Task 9 (video_cmd + gif_cmd 坐标)
  ↓
Task 12 (全量验证)
```

Task 11 和 Task 10 可以与主线并行执行。
